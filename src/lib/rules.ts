// Business rules from "PubAFric en 7 questions"
export const MISSION_DEADLINE_HOURS = 24;
export const MAX_CONCURRENT_CLAIMS = 2;
// No minimum/maximum on deposit or withdrawal requests — every request is reviewed
// by an admin who moves the real money (bank transfer / mobile money) outside the app.
